package com.aio.kotlin

import androidx.test.ext.junit.runners.AndroidJUnit4
import org.junit.Test
import org.junit.runner.RunWith

@RunWith(AndroidJUnit4::class)
class EspressoWoUnitTest {


    @Test
    fun testFragmentIsDisplayed() {
//        val scenario = launchFragmentInContainer<EspressoWoUnitTestFragment>()
//
//        // EventFragment has gone through onAttach(), but not onCreate(). Verify the initial state.
//        scenario.moveToState(Lifecycle.State.RESUMED)
    }
}